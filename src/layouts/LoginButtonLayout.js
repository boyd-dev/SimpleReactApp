import styled from 'styled-components';

const LoginButtonLayout = styled.div`
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  justify-content: center;
  & img {
    cursor: pointer;
  }
`

export {
    LoginButtonLayout
}
